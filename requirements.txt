# ==============================================================================
# NEXUS ORCHESTRATOR — requirements.txt (Runtime Only)
# Fonte canônica: pyproject.toml  |  Lock file: uv.lock
# Sincronizado para SOTA v7.0 GOLD
# ==============================================================================

# ── NUCLEO ASSINCRONO ─────────────────────────────────────────────────────────
aiofiles>=24.1.0
aiohttp>=3.11.0
aiosqlite>=0.21.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# ── PYDANTIC & SETTINGS ───────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.7.0

# ── MOTOR MATEMATICO & ANALYTICS ─────────────────────────────────────────────
numpy>=2.2.0
pandas>=2.2.0
scikit-learn>=1.6.0
scipy>=1.15.0

# ── RAG & MEMORIA VETORIAL ───────────────────────────────────────────────────
# SEGURANCA: PYSEC-2026-311 sem correcao upstream ate 1.5.9. Nao explorada
# porque so usamos chromadb.PersistentClient (embarcado, sem servidor).
# NAO migrar para HttpClient / `chroma run` sem reavaliar a advisory.
chromadb>=0.6.0
onnxruntime>=1.20.0
sentence-transformers>=3.3.0

# ── CLIENTES LLM ─────────────────────────────────────────────────────────────
google-genai>=1.2.0
openai>=1.58.0
anthropic>=0.42.0
httpx>=0.28.0

# ── INGESTAO DE DOCUMENTOS ───────────────────────────────────────────────────
pypdf>=6.12.0
python-docx>=1.2.0
openpyxl>=3.1.5
Pillow>=12.3.0

# ── MULTIMIDIA ───────────────────────────────────────────────────────────────
moviepy>=2.2.0
opencv-python-headless>=4.13.0

# ── UTILITARIOS ──────────────────────────────────────────────────────────────
regex>=2026.5.0
requests>=2.34.0
PyYAML>=6.0.2
tqdm>=4.67.0
rich>=15.0.0
python-dateutil>=2.9.0
tenacity>=9.1.0

# ── FILESYSTEM & CLOUD ───────────────────────────────────────────────────────
watchdog>=6.0.0
watchfiles>=1.2.0
cloudpathlib>=0.24.0

# ── INFRA & MONITORAMENTO ────────────────────────────────────────────────────
psutil>=7.2.0
loguru>=0.7.3
typer>=0.25.1

import AulaICMPage from './aulas/icm-masterclass/page';

// A página inicial (/) agora renderiza o conteúdo da Masterclass de ICM,
// que serve como a landing page principal do projeto.
export default function Home ()
{
  return <AulaICMPage />;
}

mcpServers:
  - name: "github-vitoihub-vitoi"
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      # Use a variável de ambiente que definimos ou cole o novo token aqui se preferir
      GITHUB_PERSONAL_ACCESS_TOKEN: ${GITHUB_TOKEN}$
d: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:3.1 Pro (Cérebro
      # Use a variável de ambiente que definimos ou cole o novo token aqui se preferir
      GITHUB_PERSON1.5-pro" # Otimizado para raciocínio complexo/poker/psicologia
SUA_CHAVE_AQUI" # Se funcionou hardcoded, mantenha assim por enquanto
models:
  - name: "Gemini 3.1 Pro (Cérebro)"
      - tool_use # Permite usar o MCP do GitHubsdk"
    apiKey: "GEMINI_API_KEY" # Se funcionou hardcoded, mantenha assim por enquanto
    roles:0 Flash (Escrita/Código
      - chat
      - tool_use # 1.5-flashCP do GitHub
SUA_CHAVE_AQUI

      - edit
      - apply
      - autocomplete
